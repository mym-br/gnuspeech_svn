#
# Generated by the NeXT Interface Builder.
#
# NOTE: Do NOT change this file -- Interface Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = degas

INTERFACES = degas.nib
CLASSES = Categories.m FileManager.m Generate.m NXImageView.m \
          PhoneDescription.m Rule.m SpecialEventView.m Synthesize.m \
          Template.m TileScrollView.m TransitionView.m
MFILES = degas_main.m
CFILES = evaluate.c parse.c reserved_symbol.c synthesizer_module.c
HFILES = dspcore.h evaluate.h parse.h reserved_symbol.h \
         synthesizer_module.h
TIFFFILES = crosshairCursor.tiff


SOURCEMODE = 444

LIBS = -lCircularSlider -ldsp_s\
	-lNeXT_s -lsys_s
DEBUG_LIBS = $(LIBS)
PROF_LIBS = -lCircularSlider -ldsp_s\
	-lNeXT_p -lsys_p

MAKEFILEDIR = /usr/lib/nib
ICONSECTIONS =	-sectcreate __ICON app /usr/lib/nib/default_app_icon.tiff

INSTALLDIR = $(HOME)/Apps
INSTALLFLAGS = -c -s -m 755

-include Makefile.preamble

include $(MAKEFILEDIR)/app.make

-include Makefile.postamble

-include Makefile.dependencies
